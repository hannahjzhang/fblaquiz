// displays the results of the quizData
import React from "react";
import "./App.css";
import { quizData } from "./data";
/*
// function called to automatically grade user answers
function Grade() {
  var quizQuestions=5
  {// create an array of answers to loop through}
  answers.map((val,index)=>{
    if (data.)
  }
}
*/
